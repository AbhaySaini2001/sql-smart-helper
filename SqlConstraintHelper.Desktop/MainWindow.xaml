<Window x:Class="SqlConstraintHelper.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:SqlConstraintHelper.Desktop.ViewModels"
        mc:Ignorable="d"
        Title="SQL Constraint &amp; Query Helper" 
        Height="800" Width="1400"
        WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <viewmodels:MainViewModel />
    </Window.DataContext>

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <!-- Style for headers -->
        <Style x:Key="HeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,10,0,5"/>
        </Style>

        <!-- Style for section panels -->
        <Style x:Key="SectionPanel" TargetType="Border">
            <Setter Property="Background" Value="#F5F5F5"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="5"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="100*"/>
            <RowDefinition Height="587*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Connection Panel -->
        <Border Grid.Row="0" Style="{StaticResource SectionPanel}" Margin="10,10,10,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="250"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="250"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Server:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox Grid.Column="1" Text="{Binding CurrentConnection.Server, UpdateSourceTrigger=PropertyChanged}" 
                         VerticalAlignment="Center" IsEnabled="{Binding IsConnected, Converter={StaticResource InverseBoolConverter}}"/>

                <TextBlock Grid.Column="2" Text="Database:" VerticalAlignment="Center" Margin="20,0,10,0"/>
                <TextBox Grid.Column="3" Text="{Binding CurrentConnection.Database, UpdateSourceTrigger=PropertyChanged}" 
                         VerticalAlignment="Center" IsEnabled="{Binding IsConnected, Converter={StaticResource InverseBoolConverter}}"/>

                <CheckBox Grid.Column="4" Content="Windows Auth" IsChecked="{Binding CurrentConnection.UseIntegratedSecurity}" 
                          VerticalAlignment="Center" Margin="20,0,0,0"
                          IsEnabled="{Binding IsConnected, Converter={StaticResource InverseBoolConverter}}"/>

                <StackPanel Grid.Column="6" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Connect" Command="{Binding ConnectCommand}" 
                            Width="100" Height="30" Margin="5,0"
                            Visibility="{Binding IsConnected, Converter={StaticResource InverseBoolToVisConverter}}"/>
                    <Button Content="Disconnect" Command="{Binding DisconnectCommand}" 
                            Width="100" Height="30" Margin="5,0"
                            Visibility="{Binding IsConnected, Converter={StaticResource BoolToVis}}"/>
                    <Button Content="Refresh" Command="{Binding RefreshCommand}" 
                            Width="100" Height="30" Margin="5,0"
                            IsEnabled="{Binding IsConnected}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="10,5,10,5" Grid.RowSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel: Tables List -->
            <Border Grid.Column="0" Style="{StaticResource SectionPanel}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Tables" Style="{StaticResource HeaderStyle}"/>

                    <DataGrid Grid.Row="1" 
                              ItemsSource="{Binding Tables}"
                              SelectedItem="{Binding SelectedTable}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              SelectionMode="Single"
                              GridLinesVisibility="None"
                              HeadersVisibility="Column">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Schema" Binding="{Binding SchemaName}" Width="60"/>
                            <DataGridTextColumn Header="Table Name" Binding="{Binding TableName}" Width="*"/>
                            <DataGridTextColumn Header="Rows" Binding="{Binding RowCount}" Width="60"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="#CCCCCC"/>

            <!-- Right Panel: Tabs -->
            <TabControl Grid.Column="2">
                <!-- Table Details Tab -->
                <TabItem Header="Table Details">
                    <Grid Margin="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="200"/>
                        </Grid.RowDefinitions>

                        <!-- Table Info -->
                        <StackPanel Grid.Row="0" Margin="0,0,0,10">
                            <TextBlock Text="{Binding SelectedTable.FullName, StringFormat='Table: {0}'}" 
                                       FontSize="18" FontWeight="Bold"/>
                            <TextBlock Text="{Binding SelectedTable.RowCount, StringFormat='Rows: {0:N0}'}" 
                                       Foreground="Gray" Margin="0,5,0,0"/>
                        </StackPanel>

                        <!-- Columns Grid -->
                        <DataGrid Grid.Row="1" 
                                  ItemsSource="{Binding SelectedTable.Columns}"
                                  AutoGenerateColumns="False"
                                  IsReadOnly="True">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Column" Binding="{Binding ColumnName}" Width="*"/>
                                <DataGridTextColumn Header="Data Type" Binding="{Binding DataType}" Width="100"/>
                                <DataGridCheckBoxColumn Header="PK" Binding="{Binding IsPrimaryKey}" Width="40"/>
                                <DataGridCheckBoxColumn Header="FK" Binding="{Binding IsForeignKey}" Width="40"/>
                                <DataGridCheckBoxColumn Header="Nullable" Binding="{Binding IsNullable}" Width="70"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <!-- Query Generation Buttons -->
                        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10">
                            <Button Content="SELECT" Command="{Binding GenerateSelectQueryCommand}" 
                                    Width="80" Height="30" Margin="0,0,5,0"/>
                            <Button Content="INSERT" Command="{Binding GenerateInsertQueryCommand}" 
                                    Width="80" Height="30" Margin="0,0,5,0"/>
                            <Button Content="UPDATE" Command="{Binding GenerateUpdateQueryCommand}" 
                                    Width="80" Height="30" Margin="0,0,5,0"/>
                            <Button Content="DELETE" Command="{Binding GenerateDeleteQueryCommand}" 
                                    Width="80" Height="30" Margin="0,0,5,0"/>
                            <Button Content="Copy Query" Command="{Binding CopyQueryCommand}" 
                                    Width="100" Height="30" Margin="20,0,0,0"/>
                        </StackPanel>

                        <!-- Generated Query -->
                        <TextBox Grid.Row="3" 
                                 Text="{Binding GeneratedQuery, Mode=OneWay}"
                                 TextWrapping="Wrap"
                                 VerticalScrollBarVisibility="Auto"
                                 FontFamily="Consolas"
                                 IsReadOnly="True"
                                 Background="#F9F9F9"/>
                    </Grid>
                </TabItem>

                <!-- Foreign Keys Tab -->
                <TabItem Header="Foreign Keys">
                    <Grid Margin="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <DataGrid Grid.Row="0"
                                  ItemsSource="{Binding ForeignKeys}"
                                  SelectedItem="{Binding SelectedForeignKey}"
                                  AutoGenerateColumns="False"
                                  IsReadOnly="True">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Constraint Name" Binding="{Binding ConstraintName}" Width="*"/>
                                <DataGridTextColumn Header="From Table" Binding="{Binding TableName}" Width="150"/>
                                <DataGridTextColumn Header="Column" Binding="{Binding ColumnName}" Width="120"/>
                                <DataGridTextColumn Header="To Table" Binding="{Binding ReferencedTable}" Width="150"/>
                                <DataGridTextColumn Header="Column" Binding="{Binding ReferencedColumn}" Width="120"/>
                                <DataGridCheckBoxColumn Header="Enabled" Binding="{Binding IsEnabled}" Width="70"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,0">
                            <Button Content="Enable/Disable Constraint" 
                                    Command="{Binding ToggleConstraintCommand}"
                                    CommandParameter="{Binding SelectedForeignKey}"
                                    Width="180" Height="30"/>
                        </StackPanel>
                    </Grid>
                </TabItem>

                <!-- Issues Tab -->
                <TabItem>
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Issues"/>
                            <Border Background="Red" CornerRadius="10" Margin="5,0,0,0" 
                                    Padding="5,0" MinWidth="20"
                                    Visibility="{Binding ErrorCount, Converter={StaticResource CountToVisibilityConverter}}">
                                <TextBlock Text="{Binding ErrorCount}" Foreground="White" 
                                           FontWeight="Bold" FontSize="10"/>
                            </Border>
                        </StackPanel>
                    </TabItem.Header>

                    <DataGrid ItemsSource="{Binding Issues}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              Margin="10">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Severity" Binding="{Binding Severity}" Width="80"/>
                            <DataGridTextColumn Header="Constraint" Binding="{Binding ConstraintName}" Width="200"/>
                            <DataGridTextColumn Header="Table" Binding="{Binding TableName}" Width="150"/>
                            <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                            <DataGridTextColumn Header="Affected" Binding="{Binding AffectedRows}" Width="80"/>
                        </DataGrid.Columns>
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Severity}" Value="Error">
                                        <Setter Property="Background" Value="#FFEBEE"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Severity}" Value="Critical">
                                        <Setter Property="Background" Value="#FFCDD2"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Severity}" Value="Warning">
                                        <Setter Property="Background" Value="#FFF9C4"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>
                    </DataGrid>
                </TabItem>
            </TabControl>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="#E0E0E0" BorderBrush="#CCCCCC" BorderThickness="0,1,0,0" Height="30" Margin="0,587,0,0" Grid.RowSpan="2">
            <Grid Margin="10,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <ProgressBar Width="100" Height="15" IsIndeterminate="True" 
                                 Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"
                                 Margin="0,0,10,0"/>
                    <TextBlock Text="{Binding Tables.Count, StringFormat='Tables: {0}'}" 
                               VerticalAlignment="Center" Margin="10,0"/>
                    <TextBlock Text="{Binding ForeignKeys.Count, StringFormat='FKs: {0}'}" 
                               VerticalAlignment="Center" Margin="10,0"/>
                    <Border Background="Red" CornerRadius="3" Padding="5,2" Margin="10,0,0,0"
                            Visibility="{Binding ErrorCount, Converter={StaticResource CountToVisibilityConverter}}">
                        <TextBlock Text="{Binding ErrorCount, StringFormat='Errors: {0}'}" 
                                   Foreground="White" FontWeight="Bold"/>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Loading Overlay -->
        <Border Grid.RowSpan="4" Background="#80000000" 
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar Width="200" Height="20" IsIndeterminate="True"/>
                <TextBlock Text="Loading..." Foreground="White" FontSize="16" 
                           Margin="0,10,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>